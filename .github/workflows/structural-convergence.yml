name: SONAR structural convergence backtest
on:
  push:
    paths:
      - 'scripts/structural-convergence.mjs'
      - '.github/workflows/structural-convergence.yml'
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Prepare ESM imports for Node
        run: |
          sed -i "s|'../engine/scoring'|'../engine/scoring.js'|g; s|'./scoring'|'./scoring.js'|g; s|'./dominant-band'|'./dominant-band.js'|g" src/engine/multigen.js src/engine/dominant-band.js
      - run: node scripts/structural-convergence.mjs
      - name: Commit report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add SONAR_structural_convergence_backtest_24settembre2026.md
          git diff --staged --quiet || git commit -m "Backtest structural convergence SONAR"
          git push
